---
// Component for the header section
const personalName = "Justin Cui"; // Placeholder - CHANGE THIS!
const location = "Toronto"; // Placeholder - ADD YOUR LOCATIONS
// Ensure image URL respects BASE_URL (for GitHub Pages)
const baseUrl = import.meta.env.BASE_URL || '/';
const profilePicSrc = baseUrl.endsWith('/') ? `${baseUrl}profile.png` : `${baseUrl}/profile.png`;

const navLinks = [
  { href: "#about", text: "About" },
  { href: "#experience", text: "Experience" },
  { href: "#projects", text: "Projects" },
  { href: "#publications", text: "Publications" },
];
const socialLinks = [
  { href: "#", text: "CV", icon: "📄" }, // Placeholder link - CHANGE THIS!
  { href: "https://github.com/cuijustin0617", text: "GitHub", icon: "<svg width=\"20\" height=\"20\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" class=\"feather feather-github\"><path d=\"M9 19c-5 1.5-5-2.5-7-3m14 6v-3.87a3.37 3.37 0 0 0-.94-2.61c3.14-.35 6.44-1.54 6.44-7A5.44 5.44 0 0 0 20 4.77 5.07 5.07 0 0 0 19.91 1S18.73.65 16 2.48a13.38 13.38 0 0 0-7 0C6.27.65 5.09 1 5.09 1A5.07 5.07 0 0 0 5 4.77a5.44 5.44 0 0 0-1.5 3.78c0 5.42 3.3 6.61 6.44 7A3.37 3.37 0 0 0 9 18.13V22\"></path></svg>" }, // Placeholder link - CHANGE THIS!
  { href: "https://www.linkedin.com/in/justin-cui-b7b654181/", text: "LinkedIn", icon: "<svg width=\"20\" height=\"20\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" class=\"feather feather-linkedin\"><path d=\"M16 8a6 6 0 0 1 6 6v7h-4v-7a2 2 0 0 0-2-2 2 2 0 0 0-2 2v7h-4v-7a6 6 0 0 1 6-6z\"></path><rect x=\"2\" y=\"9\" width=\"4\" height=\"12\"></rect><circle cx=\"4\" cy=\"4\" r=\"2\"></circle></svg>" } // Placeholder link - CHANGE THIS!
];
---

<header class="container site-header">
  <div class="header-top-row">
    <div class="profile-pic-header">
      <img src={profilePicSrc} alt="Justin Cui" />
    </div>
    <div class="header-main-text">
      <h1 class="site-name">{personalName}</h1>
      <p class="location-info">{location}</p> 
    </div>
  </div>
  <nav class="main-nav">
    <ul>
      {navLinks.map(link => <li><a href={link.href}>{link.text}</a></li>)}
    </ul>
  </nav>
  <div class="social-links-container">
    {socialLinks.map(link => <a href={link.href} title={link.text} target="_blank" rel="noopener noreferrer" set:html={link.icon}><span class="sr-only">{link.text}</span></a>)}
  </div>
</header>

<style>
  .site-header {
    padding-top: 2rem; /* Adjusted padding */
    padding-bottom: 1rem;
    border-bottom: 1px solid var(--gray-light);
    margin-bottom: 2rem;
    text-align: left; /* Align header content to left */
  }
  .header-top-row {
    display: flex;
    align-items: center; /* Vertically align pic and text block */
    gap: 1rem; /* Space between picture and text */
    margin-bottom: 0.75rem; /* Space before nav */
  }
  .profile-pic-header {
    width: 60px; /* Adjust size as needed */
    height: 60px;
    border-radius: 50%;
    overflow: hidden; /* Ensure image stays circular */
    flex-shrink: 0; /* Prevent shrinking */
  }
  .profile-pic-header img {
    width: 100%;
    height: 100%;
    object-fit: cover; /* Crop image to fit circle */
  }
  .header-main-text {
    /* flex-grow: 1; /* Allow text to take remaining space */
  }
  .site-name {
    font-size: 1.8rem; /* Adjusted for potentially less space */
    font-weight: 700; /* Bolder */
    margin: 0 0 0.1rem 0; /* Adjusted margin */
    color: var(--black);
  }
  .location-info {
    font-size: 0.85rem; /* Adjusted size */
    color: var(--gray-dark);
    margin: 0;
  }
  .main-nav ul {
    list-style: none;
    padding: 0;
    margin: 0 0 0.75rem 0; /* Margin below nav */
    display: flex;
    flex-wrap: wrap; /* Allow nav links to wrap */
    gap: 0.5rem 1.2rem; /* Row and column gap */
  }
  .main-nav a {
    font-size: 0.9rem; /* Slightly smaller nav links */
    font-weight: 500;
    color: var(--text-color); /* Use default text color */
    text-decoration: none;
    padding: 0.25rem 0; /* Add some padding for touch targets / visual spacing */
  }
  .main-nav a:hover {
    color: var(--text-color); /* Keep same color on hover, no underline */
    /* text-decoration: underline; /* Optional: if underline on hover is desired for nav */
  }
  .social-links-container {
    display: flex;
    gap: 0.75rem;
    align-items: center; /* Align icons nicely if they have different heights */
  }
  .social-links-container a {
    color: var(--gray-dark); /* Social icons color */
    display: inline-block; /* For better icon rendering */
  }
  .social-links-container a:hover {
    color: var(--black); /* Darker on hover */
  }
  .social-links-container a svg {
    vertical-align: middle;
  }
  .sr-only {
    position: absolute;
    width: 1px;
    height: 1px;
    padding: 0;
    margin: -1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    white-space: nowrap;
    border-width: 0;
  }
</style> 
